#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Two number combinations'
 *
 * Return: 0(Success)
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('n\');
	return (0);
}
